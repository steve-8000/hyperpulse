# Metal 운영 리포트

- 생성 시각: 2026-02-22T01:39:05.302Z
- RSS: Etna.2
- 비교 기준(Base): 71184418ae952b5b81966fef875173245dda9d24
- 최신 커밋(Head): 2b9d8ec9cc802530b54a005a8f7bb14f7d757d87

## 한줄 요약
본 커밋은 메타 블록체인 프로젝트의 CI/빌드 환경을 Ubuntu 20.04(‘focal’)에서 Ubuntu 22.04(‘jammy’)로 전환한 내용이다. 주요 변경사항은 빌드 워크플로우에서 사용되는 베이스 이미지와 릴리즈 이름을 ‘focal’ → ‘jammy’ 으로 교체한 점이며, 이는 운영 환경(빌드 서버)과 문서에 반영된다. 프로토콜 레이어에서는 RPC/API/JSON‑RPC 인터페이스가 변하지 않으므로, 노드 운영자는 별도의 API 변경을 인지할 필요는 없다. 이 커밋은 단순히 빌드 환경을 최신화한 것이며, 메타 프로토콜 자체에 대한 기능적 변화는 없다. 아래 항목들은 운영 관점에서 주의해야 할 사항을 정리하였다.

## 운영자 중요 체크
- 아카이브 노드 운영(보관/색인/동기화) 관련 변경 단서가 감지되었습니다.
- 노드 설정값/프로토콜 플래그 관련 변경 단서가 감지되었습니다.

## 주요 변경점
- Ubuntu 20.04(‘focal’) 지원 중단 및 Ubuntu 22.04(‘jammy’)로 전환
- 빌드 워크플로우에서 `RELEASE` 값이 ‘focal’ → ‘jammy’ 으로 교체
- README에서 OS 요구사항이 `Ubuntu 20.04/22.04` → `Ubuntu 22.04` 으로 수정

## RPC/API 영향
- 해당 사항 없음

## 아카이브 노드 영향
- 해당 사항 없음

## 운영 액션 아이템
- 해당 사항 없음

## 마이그레이션 체크리스트
- 해당 사항 없음

## 위험/주의 사항
- 위험 요소 없음

## 근거(Evidence)
- commit 2b9d8ec9: `build-linux-binaries.yml`에서 `runs-on: ubuntu-20.04` → `ubuntu-22.04` 교체.
- commit 2b9d8ec9: `RELEASE: "focal"` → `RELEASE: "jammy"` 교체.
- commit 2b9d8ec9: `README.md`에서 OS 요구사항이 `Ubuntu 20.04/22.04` → `Ubuntu 22.04` 으로 교체.
- commit 2b9d8ec9: `build-ubuntu-amd64-release.yml`에서 `build-focal-amd64-package` → `build-jammy-amd64-package` 교체.
- commit 2b9d8ec9: `build-ubuntu-amd64-release.yml`에서 패키지 이름이 `focal` → `jammy` 으로 교체.

## 운영 메모
- 추가 메모 없음

## 원문 커밋 내용
### Commit Log
```text
2b9d8ec9c deprecate ubuntu 20.04
```

