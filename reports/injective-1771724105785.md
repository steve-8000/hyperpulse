# Injective 운영 리포트

- 생성 시각: 2026-02-22T01:35:05.785Z
- RSS: v1.17.2-basefee-1769515040
- 비교 기준(Base): 5032cb6b66052acd9f85f4c4c8ba187695874061
- 최신 커밋(Head): a3a0a2c5245e3836f11f6dd74fc2ad974814d909

## 한줄 요약
Injective v1.17.2는 메인넷 업그레이드 버전으로, 주된 변경사항은 Docker 환경 설정에서 INJ_CORE_IMAGE_VERSION을 v1.17.1 → v1.17.2 로 업데이트한 것뿐이다. 이 변경은 주로 컨테이너 이미지 버전을 지정하는 환경 변수에 반영되며, 실제 코어 코드나 RPC 인터페이스에는 직접적인 변경이 없다.

## 운영자 중요 체크
- 아카이브 노드 운영(보관/색인/동기화) 관련 변경 단서가 감지되었습니다.

## 주요 변경점
- 주요 변경점 없음

## RPC/API 영향
- 해당 사항 없음

## 아카이브 노드 영향
- 아카이브 노드에 대한 직접적인 영향은 없다. 이미지 버전 변경만으로는 스토리지 정책이나 색인에 변동이 없으므로, 기존 아카이브 노드 운영은 그대로 유지된다.

## 운영 액션 아이템
- docker/.env.mainnet.example 파일에서 INJ_CORE_IMAGE_VERSION 값을 v1.17.2 로 업데이트
- 컨테이너 이미지 풀링 후 재배포(또는 Swarm 모드에서 서비스 재시작)
- 배포 후 health‑check 및 로그 모니터링(특히 컨테이너 시작 시 에러 여부)

## 마이그레이션 체크리스트
- 이미지 태그가 v1.17.2 로 정확히 지정되었는지 확인
- 컨테이너 재배포 전 이미지 풀링 테스트(이미지 존재 여부)
- 업그레이드 후 RPC/REST API가 정상 동작하는지 테스트
- 아카이브 노드에 대한 영향이 없으므로 별도 마이그레이션 스크립트 필요 없음

## 위험/주의 사항
- 현재 커밋에서는 코드베이스에 큰 변동이 없으나, 이미지 버전 변경은 컨테이너 배포 시 이미지 풀링 정책에 따라야 한다. 만일 새 버전이 이전 버전과 호환되지 않는 패키지 의존재가 있다면, 컨테이너 재배포 시 다운타임이 발생할 수 있다.
- 업그레이드 전 테스트 환경에서 이미지 버전 충돌을 확인하고, 기존 노드와의 호환성 검증이 필요하다.

## 근거(Evidence)
- 근거 데이터 없음

## 운영 메모
- 이 커밋은 단순 환경 변수 업데이트이며, RPC/API/JSON‑RPC 인터페이스에 직접적인 변경이 없다. 따라서 기존 API 호출은 그대로 동작한다.
- 컨테이너 이미지 버전 변경에 따라, Docker Compose/Swarm 설정을 재검토하고, 새 이미지를 pull 후 배포 시 이미지 태그를 정확히 지정해야 한다.

## 원문 커밋 내용
### Commit Log
```text
a3a0a2c feat: mainnet chain upgrade v1.17.2
```

