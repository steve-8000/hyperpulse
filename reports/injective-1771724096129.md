# Injective 운영 리포트

- 생성 시각: 2026-02-22T01:34:56.129Z
- RSS: v1.17.2
- 비교 기준(Base): d61ae0c32e1110da692992a7849bca4b0ec7dd07
- 최신 커밋(Head): 5032cb6b66052acd9f85f4c4c8ba187695874061

## 한줄 요약
본 릴리즈는 Injective 코어 이미지 버전을 v1.17.2-beta 로 업데이트한 단순 패치이며, Docker 환경 설정 파일에만 반영됩니다. 핵심 로직이나 RPC/JSON‑RPC 인터페이스에는 변동이 없으므로, 운영자는 기존 인프라를 그대로 유지하면서 이미지 버전만 교체하면 됩니다.

## 운영자 중요 체크
- 아카이브 노드 운영(보관/색인/동기화) 관련 변경 단서가 감지되었습니다.

## 주요 변경점
- 주요 변경점 없음

## RPC/API 영향
- 해당 사항 없음

## 아카이브 노드 영향
- 해당 사항 없음

## 운영 액션 아이템
- docker-compose.yml 혹은 docker/.env 파일을 최신 버전으로 교체
- 컨테이너 재시작: docker‑compose up -d --force-recreate
- 컨테이너 로그를 모니터링하여 정상 동작 여부 확인
- 메인넷/테스트넷 두 환경 모두에서 이미지 태그가 정상적으로 pull 되는지 검증

## 마이그레이션 체크리스트
- 새 이미지 태그(v1.17.2-beta)가 레지스트리에서 존재하는지 확인
- docker/.env 파일에 반영된 버전이 실제 컨테이너에서 사용되는지 테스트
- 배포 전 백업 및 롤백 포인트 설정(이미지 버전이 실패 시 즉시 복구)
- 모니터링 지표(컨테이너 상태, CPU/메모리 사용량) 재확인

## 위험/주의 사항
- 이미지 태그가 변경되었으므로, 기존 컨테이너를 재배포할 때 새 버전이 정상적으로 pull 되는지 확인 필요
- 버전 업그레이드가 테스트넷과 메인넷에 동시에 적용되므로, 두 환경의 설정 파일을 일관성 있게 업데이트해야 함

## 근거(Evidence)
- 근거 데이터 없음

## 운영 메모
- 코어 버전이 v1.17.2-beta 로 바뀐 점을 반영한 환경 변수만 수정되었습니다.
- RPC/API/JSON‑RPC 레이어에는 변경이 없으므로, 기존 엔드포인트는 그대로 사용 가능
- 아카이브 노드는 이미지 버전 변경에 따라 재시작 필요는 없으며, 데이터베이스/스토리지 구조 변화가 없음

## 원문 커밋 내용
### Commit Log
```text
5032cb6 feat: update testnet core version to v1.17.2-beta
a241939 feat: v1.17.1 upgrade
```

