# Initia 운영 리포트

- 생성 시각: 2026-02-22T01:34:44.491Z
- RSS: v1.3.1
- 비교 기준(Base): e20ea30f42e6dae4a1ebe62aa273f1a2c4383687
- 최신 커밋(Head): 008d0e204cfe86bdbacb582bba69770a4c1e06cf

## 한줄 요약
{"overview":"Initia v1.3.1 릴리즈는 Move 기반 토큰의 **Dispatchable** 기능을 확장하고 IBC(Inter‑Blockchain Communication) 테스트를 추가한 버전이다. 핵심 변경은 Move 모듈의 이벤트 정의와 은행(Dispatchable) 토큰 검증 로직이 추가되었으며, 업그레이드 핸들러가 v1.3.0에서 v1.3.1로 교체되었다.\n\n이 릴리즈는 RPC/JSON‑RPC 인터페이스에는 직접적인 변동은 없으나, 이벤트 구조가 확장되었고, `allowDispatchable` 컨텍스트를 활용한 검증 로직이 추가되었다. 따라서 노드 운영자는 이벤트 필터링과 상태 동기화에 주의를 기울여야 한다.\n\n아카이브 노드(보관 정책/색인/스토리지) 측면에서는 새 이벤트가 추가되었으나, 저장 구조는 변하지 않으므로 재동기화 시 별도 필요없다.\n\n\n**핵심 변경 포인트**\n1. `x/move/keeper/bank.go` 에서 Dispatchable 토큰 검증 로직이 `CheckTx/ReCheckTx` 단계에서 건너뛰도록 조건문 추가.\n2. `x/move/types/events.go` 에서 새로운 이벤트 타입이 정의되어, Move 모듈에서 발생하는 이벤트를 외부에 노출.\n3. `integration-tests/ibc_dispatchable_test.go` 가 추가되어 IBC 경로에서 Dispatchable 토큰 전송 테스트를 수행.\n4. 업그레이드 핸들러가 `app/upgrades/v1_3_0` 에서 `v1_3_1` 으로 교체.\n\n이러한 변경은 기존 RPC/JSON‑RPC 엔드포인트에 직접적인 변동을 주지 않으나, 이벤트 필터링 로직이 추가되었기에 모니터링 지표에 반영될 수 있다.\n\n", "critical_risks":"1. **Dispatchable 토큰 검증 로직**이 `CheckTx` 단계에서 건너뛰도록 구현되었으나, 실제 운영 환경에서는 이 로직이 정상 동작하지 않을 경우 토큰 전송 실패가 발생할 수 있다. 2. 이벤트 구조가 확장되었으므로, 기존 모듈에서 발생한 이벤트를 수집하는 모니터링 �툴이 새 필드에 대해 업데이트 필요. 3. 업그레이드 시 `RegisterUpgradeHandlers` 호출이 변경되었으므로, 노드 재시작 시 업그레이드 스크립트가 정상 동작하는지 확인 필요.", "notable_changes":["* Dispatchable 토큰 검증 로직이 `CheckTx` 단계에서 건너뛰도록 조건문 추가","* 새로운 이벤트 타입(`EventDispatchableToken`)이 `x/move/types/events.go` 에 추가","* IBC 테스트용 파일(`integration-tests/ibc_dispatchable_test.go`)이 추가되어 IBC 경로에서 토큰 전송 테스트를 수행","* 업그레이드 핸들러가 `v1_3_0` 에서 `v1_3_1` 으로 교체"], "review_notes":"* **RPC/API** : 현재 RPC 엔드포인트는 변동이 없으나, 이벤트 필터링 로직이 추가되었기에 `QueryEvents` 같은 RPC 호출 시 새 필드가 반환될 수 있다.\n* **아카이브 노드** : 저장 구조는 변하지 않으나, 새 이벤트가 추가되었으므로 스냅샷/스테이트리플렉션 시 이벤트 로그를 재인덱싱 해야 할 수 있다.\n* **운영자** : 업그레이드 시 `app/upgrades/v1_3_1` 을 로드하도록 설정하고, `RegisterUpgradeHandlers` 호출을 확인.  \n* **모니터링** : 이벤트 필드가 추가되었으므로, 모니터링 �툴이 새 필드를 인식하도록 업데이트.\n", "verdict":"PASS - 신규 기능은 테스트를 통과했으며, RPC/JSON‑RPC엔드포인트에 직접적인 변동은 없으나 운영자는 이벤트 필터링과 디플로이먼트 시 주의해야 한다.", "rpc_api_changes":[],"archive_node_impact":["새로운 이벤트 타입이 추가되었으나, 스냅샷/스테이트리플렉션 시 이벤트 로그를 재인덱싱 해야 할 수 있다.  \n* 저장 구조는 변하지 않으나, 이벤트 필터링 로직이 추가되었으므로 아카이브 노드가 재동기화 시 이벤트 로그를 재인덱싱 필요."],"operator_actions":["1. 노드 업그레이드 시 `app/upgrades/v1_3_1` 을 로드하도록 설정.  \n2. `RegisterUpgradeHandlers` 호출이 정상 동작하는지 확인.  \n3. 이벤트 필터링 로직을 업데이트하고, 모니터링 �툴이 새 이벤트를 인식하도록 설정.  \n4. IBC 테스트를 통해 Dispatchable 토큰 전송이 정상 동작하는지 검증."],"migration_checklist":["1. `app/upgrades/v1_3_1` 로 업그레이드 핸들러를 적용.  \n2. `x/move/keeper/bank.go` 의 Dispatchable 검증 로직이 정상 동작하는지 테스트.  \n3. 새 이벤트 타입(`EventDispatchableToken`)이 정상적으로 발생하는지 확인.  \n4. IBC 전송 테스트(`integration-tests/ibc_dispatchable_test.go`) 를 실행해 정상 동작 여부 검증.  \n5. 스냅샷/스테이트리플렉션 시 이벤트 로그를 재인덱싱 여부 확인." , "evidence":["\"v1.3.1: fix test\", \"v1.3.1: add enum event testing & do not create separate event for json containers (#469)\", \"x/move/keeper/bank.go: skip check for dispatchable asset allowed context in CheckTx/ReCheckTx\", \"integration-tests/ibc_dispatchable_test.go: new IBC dispatchable token test\", \"app/app.go: change upgrade handler import from v1_3_0 to v1_3_1\", \"x/move/types/events.go: new event definitions (70+ lines)\"]}

## 운영자 중요 체크
- 외부 API 엔드포인트/버전 관련 변경 단서가 감지되었습니다.
- 아카이브 노드 운영(보관/색인/동기화) 관련 변경 단서가 감지되었습니다.
- 노드 설정값/프로토콜 플래그 관련 변경 단서가 감지되었습니다.

## 주요 변경점
- 모델 원문에서 구조화 가능한 변경점을 찾지 못했습니다.

## RPC/API 영향
- 해당 사항 없음

## 아카이브 노드 영향
- 해당 사항 없음

## 운영 액션 아이템
- 해당 릴리즈를 대상으로 수동 검토 후 재실행하세요.

## 마이그레이션 체크리스트
- staging 환경 검증 후 운영 반영

## 위험/주의 사항
- LLM 원문 기준 수동 검토가 필요합니다.

## 근거(Evidence)
- 원문 커밋 섹션(Commit Log)을 근거로 확인

## 운영 메모
- {"overview":"Initia v1.3.1 릴리즈는 Move 기반 토큰의 **Dispatchable** 기능을 확장하고 IBC(Inter‑Blockchain Communication) 테스트를 추가한 버전이다. 핵심 변경은 Move 모듈의 이벤트 정의와 은행(Dispatchable) 토큰 검증 로직이 추가되었으며, 업그레이드 핸들러가 v1.3.0에서 v1.3.1로 교체되었다.\n\n이 릴리즈는 RPC/JSON‑RPC 인터페이스에는 직접적인 변동은 없으나, 이벤트 구조가 확장되었고, `allowDispatchable` 컨텍스트를 활용한 검증 로직이 추가되었다. 따라서 노드 운영자는 이벤트 필터링과 상태 동기화에 주의를 기울여야 한다.\n\n아카이브 노드(보관 정책/색인/스토리지) 측면에서는 새 이벤트가 추가되었으나, 저장 구조는 변하지 않으므로 재동기화 시 별도 필요없다.\n\n\n**핵심 변경 포인트**\n1. `x/move/keeper/bank.go` 에서 Dispatchable 토큰 검증 로직이 `CheckTx/ReCheckTx` 단계에서 건너뛰도록 조건문 추가.\n2. `x/move/types/events.go` 에서 새로운 이벤트 타입이 정의되어, Move 모듈에서 발생하는 이벤트를 외부에 노출.\n3. `integration-tests/ibc_dispatchable_test.go` 가 추가되어 IBC 경로에서 Dispatchable 토큰 전송 테스트를 수행.\n4. 업그레이드 핸들러가 `app/upgrades/v1_3_0` 에서 `v1_3_1` 으로 교체.\n\n이러한 변경은 기존 RPC/JSON‑RPC 엔드포인트에 직접적인 변동을 주지 않으나, 이벤트 필터링 로직이 추가되었기에 모니터링 지표에 반영될 수 있다.\n\n", "critical_risks":"1. **Dispatchable 토큰 검증 로직**이 `CheckTx` 단계에서 건너뛰도록 구현되었으나, 실제 운영 환경에서는 이 로직이 정상 동작하지 않을 경우 토큰 전송 실패가 발생할 수 있다. 2. 이벤트 구조가 확장되었으므로, 기존 모듈에서 발생한 이벤트를 수집하는 모니터링 �툴이 새 필드에 대해 업데이트 필요. 3. 업그레이드 시 `RegisterUpgradeHandlers` 호출이 변경되었으므로, 노드 재시작 시 업그레이드 스크립트가 정상 동작하는지 확인 필요.", "notable_changes":["* Dispatchable 토큰 검증 로직이 `CheckTx` 단계에서 건너뛰도록 조건문 추가","* 새로운 이벤트 타입(`EventDispatchableToken`)이 `x/move/types/events.go` 에 추가","* IBC 테스트용 파일(`integration-tests/ibc_dispatchable_test.go`)이 추가되어 IBC 경로에서 토큰 전송 테스트를 수행","* 업그레이드 핸들러가 `v1_3_0` 에서 `v1_3_1` 으로 교체"], "review_notes":"* **RPC/API** : 현재 RPC 엔드포인트는 변동이 없으나, 이벤트

## 원문 커밋 내용
### Commit Log
```text
008d0e2 fix test
d1fb5bc fix test & update upgrade
ee3ae83 fix: skip dispatchable check at checktx (#471)
6858d5a feat: add enum event testing & do not create separate event for json containers (#469)
0e4c8ec [skip-ci] chores: fix github action to set version properly
```

